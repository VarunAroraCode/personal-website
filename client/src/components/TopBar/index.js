import React from 'react'
import { TopBarContainer,Icon,CloseIcon } from './Elements'

const TopBar = () => {
    return (
        <TopBarContainer>
            <Icon>
                <CloseIcon/>
            </Icon>
        </TopBarContainer>
    )
}

export default TopBar
